개선 사항 생각난 것
1. 오늘의 한줄 요약 현재 제목 나열 -> 이를 어떻게 바꿀 수 있을지
2. 뉴스 카드에 네이버 api에서 제공해주는 description을 전부 보여줘야 할지?

frontend 실행
- npm install
- set NEXT_PRIVATE_DEBUG_CACHE=1 && npm run dev

backend 실행
- pip install -r requirements.txt
- uvicorn main:app --reload --port 8000